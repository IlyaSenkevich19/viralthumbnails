# Loaded only when you run `next dev` (NODE_ENV=development).
# Vercel production builds use NODE_ENV=production — this file is ignored there.
#
# `.env.local` (from root `yarn dev` sync) overrides these keys if both set.

NEXT_PUBLIC_BACKEND_URL=http://localhost:3001
