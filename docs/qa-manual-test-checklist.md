# Ручной QA — ViralThumbnails

**Интерактивная версия (только `development`):** откройте в браузере [`/qa-checklist`](http://localhost:3000/qa-checklist) — галочки сохраняются в `localStorage` этого браузера.

В **production** маршрут отключён (404). Исходник кейсов: `apps/frontend/src/lib/qa/qa-test-cases.ts` — при добавлении сценариев обновляйте и этот файл, и константу.

---

## Auth & shell

- [ ] Вход (email или Google) ведёт на `/create`
- [ ] Новый пользователь: после входа показывается **модалка квала** (`LeadQualificationGate`); после отправки — CRM получает строку (если задан `LEAD_INTAKE_WEBHOOK_URL`), `GET /api/auth/me` отдаёт `leadQualificationCompleted: true`
- [ ] `/auth/register` — короткая форма (Google / email+пароль), без длинного мастера на странице
- [ ] Header: кредиты без «прыжка» вёрстки, кнопка Create, после reload ок
- [ ] Sidebar: подсветка текущего раздела

## Create (`/create`)

- [ ] Prompt: пустой ввод — ошибка; успех → черновик проекта и переход в `/projects/:id/variants`
- [ ] YouTube: валидная ссылка — пайплайн, редирект в проект, тост
- [ ] YouTube: невалидный URL — ошибка, проект не создаётся
- [ ] Видео: длинный файл — сообщение об обрезке; проект + пайплайн; редирект
- [ ] Недостаточно кредитов — блокировка / paywall по текущей логике

## Projects (список)

- [ ] Список грузится; обложки и названия; refresh
- [ ] «Новый проект» (если есть) ведёт в согласованный флоу

## Project detail (`/projects/:id/variants`)

- [ ] **Header:** длинное имя проекта **обрезается**; полный текст по **hover** (`title`)
- [ ] **Source:** для видео/YouTube — URL или имя файла; статус пайплайна при наличии `pipeline_job_id`
- [ ] Пайплайн в работе: прогресс; кнопка Generate недоступна; по завершении — обновление / разблокировка
- [ ] Шаблоны: ниши, фильтр All / With face / Faceless, сетка, пагинация
- [ ] Персонаж + «Prioritize»: в режиме Faceless референс лица не уходит, подсказка в UI
- [ ] Generate: списание кредитов, появление вариантов, ошибки
- [ ] Превью, выбор в полоске, скачать, удаление с подтверждением

## Templates & My faces

- [ ] Шаблоны: сетка, картинки, фильтр по нише
- [ ] My faces: список / загрузка (как в продукте)

## Credits

- [ ] `/credits` — баланс, история, ссылка назад

## Settings & admin

- [ ] Settings: секции, app connection
- [ ] Админ YouTube inspiration: не-админ уходит; админ заходит (если настроено)

## Pipeline & recovery

- [ ] Перезагрузка страницы во время пайплайна: индикаторы / статус где реализовано
- [ ] Повторный тяжёлый job при активном: поведение по продукту (ошибка / блок)

---

## Примечание по миграциям

Порядок и назначение SQL — см. [`../supabase/migrations/README.md`](../supabase/migrations/README.md). Уже применённые миграции в репозитории **не удаляют**.
